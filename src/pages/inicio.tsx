import React from 'react';

export default function Início() {
  return (
    <div>
      {/* Apenas conteúdo principal, sem título ou mensagem duplicados */}
      <p>Conteúdo principal da página inicial.</p>
    </div>
  );
}